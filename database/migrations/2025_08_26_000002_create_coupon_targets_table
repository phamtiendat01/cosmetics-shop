<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    public function up(): void
    {
        Schema::create('coupon_targets', function (Blueprint $t) {
            $t->id();
            $t->foreignId('coupon_id')->constrained()->cascadeOnDelete();
            $t->enum('target_type', ['category', 'brand', 'product']);
            $t->unsignedBigInteger('target_id');
            $t->boolean('is_excluded')->default(false);
            $t->timestamps();

            $t->index(['coupon_id', 'target_type', 'target_id']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('coupon_targets');
    }
};
